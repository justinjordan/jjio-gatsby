import React from 'react'
import StaticQuery from 'gatsby'
import Grid from '@material-ui/core/Grid'

const getItems = () => {
  const items = []
  return items
}

const SocialIcons = () => <></>

// const SocialIcons = () => (
//   <StaticQuery
//     query={graphql``}
//     render={data => <Grid container spacing={3} children={getItems(data)}/>}
//   />
// )

export default SocialIcons